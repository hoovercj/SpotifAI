import React from "react"

/**
 * Phase 1 placeholder — replaced by the real implementation in Phase 5.
 */
export default function SearchTab() {
  return (
    <section className="flex flex-col gap-6 px-4 py-6">
      <header>
        <h1 className="text-2xl font-bold tracking-tight">Search</h1>
        <p className="text-sm text-muted-foreground">
          Find artists, albums, tracks, and podcasts.
        </p>
      </header>
      <div className="rounded-xl border border-dashed border-border/60 bg-card/40 p-6 text-center text-sm text-muted-foreground">
        Search content lands in Phase 5.
      </div>
    </section>
  )
}
