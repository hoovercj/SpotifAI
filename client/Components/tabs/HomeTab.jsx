import React from "react"

/**
 * Phase 1 placeholder — replaced by the real implementation in Phase 4.
 */
export default function HomeTab() {
  return (
    <section className="flex flex-col gap-6 px-4 py-6">
      <header>
        <h1 className="text-2xl font-bold tracking-tight">Home</h1>
        <p className="text-sm text-muted-foreground">
          Your top tracks, recent listens, and stations.
        </p>
      </header>
      <div className="rounded-xl border border-dashed border-border/60 bg-card/40 p-6 text-center text-sm text-muted-foreground">
        Home content lands in Phase 4.
      </div>
    </section>
  )
}
