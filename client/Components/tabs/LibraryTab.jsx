import React from "react"

/**
 * Phase 1 placeholder — replaced by the real implementation in Phase 3.
 */
export default function LibraryTab() {
  return (
    <section className="flex flex-col gap-6 px-4 py-6">
      <header>
        <h1 className="text-2xl font-bold tracking-tight">Library</h1>
        <p className="text-sm text-muted-foreground">
          Your saved tracks, albums, podcasts, and artists.
        </p>
      </header>
      <div className="rounded-xl border border-dashed border-border/60 bg-card/40 p-6 text-center text-sm text-muted-foreground">
        Library content lands in Phase 3.
      </div>
    </section>
  )
}
